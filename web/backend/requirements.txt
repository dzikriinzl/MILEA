# ═══════════════════════════════════════════════════════════════════════════
# M-ILEA Backend Dependencies
# ═══════════════════════════════════════════════════════════════════════════

# ── FastAPI & ASGI ───────────────────────────────────────────────────────
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6          # For file uploads
aiofiles==23.2.1                 # Async file I/O

# ── Data Models & Validation ─────────────────────────────────────────────
pydantic==2.5.3
pydantic-settings==2.1.0

# ── Session & State Management ───────────────────────────────────────────
# redis==5.0.1                   # Uncomment for production (persistent sessions)
# sqlalchemy==2.0.25             # Uncomment for DB-backed sessions

# ── Analysis Pipeline Dependencies (Phase 0 - already implemented) ───────
# NOTE: These are placeholders - actual dependencies from your existing
# core/ and engines/ modules should be listed here

# ── External Tools (must be installed system-wide) ──────────────────────
# apktool (https://ibotpeaches.github.io/Apktool/)
# jadx (https://github.com/skylot/jadx)
# bundletool (https://github.com/google/bundletool)
# adb (Android Debug Bridge - from Android SDK Platform Tools)

# ── Testing ──────────────────────────────────────────────────────────────
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0                    # For testing FastAPI endpoints

# ── Development ──────────────────────────────────────────────────────────
black==24.1.1                    # Code formatter
ruff==0.1.14                     # Fast linter
