# ═══════════════════════════════════════════════════════════════════════════
# M-ILEA Backend Dependencies
# ═══════════════════════════════════════════════════════════════════════════

# ── FastAPI & ASGI ───────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6          # For file uploads
aiofiles>=23.2.1                 # Async file I/O

# ── Data Models & Validation ─────────────────────────────────────────────
pydantic>=2.5.3
pydantic-settings>=2.1.0

# ── Optional Analysis Dependencies ───────────────────────────────────────
google-play-scraper>=1.2.7       # Play Store metadata lookup (optional, graceful fallback)
# androguard>=3.4.0              # Call-graph analysis (optional, large dependency)

# ── External Tools (must be installed system-wide) ──────────────────────
# apktool  — https://ibotpeaches.github.io/Apktool/
# jadx     — https://github.com/skylot/jadx
# aapt2    — Android SDK build-tools
# adb      — Android Debug Bridge (optional, for device analysis)

# ── Testing ──────────────────────────────────────────────────────────────
pytest>=7.4.4
pytest-asyncio>=0.23.3
httpx>=0.26.0                    # For testing FastAPI endpoints
